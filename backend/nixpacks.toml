[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["corepack enable && corepack prepare pnpm@10.28.1 --activate && pnpm install"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "node dist/index.js"=